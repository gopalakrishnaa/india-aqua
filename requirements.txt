# Serverless (Vercel) runtime deps. A slim subset of pyproject.toml.
# No playwright/streamlit/pandas/plotly/uvicorn: not used by the API app,
# and the scraper falls back to demo mode without playwright.
fastapi>=0.115
pydantic>=2.7
pydantic-settings>=2.4
sqlalchemy>=2.0
psycopg[binary]>=3.2
openai>=1.40
slowapi>=0.1.9
python-multipart>=0.0.9
httpx>=0.27
tenacity>=8.5
typer>=0.12
rich>=13.7
